name: Validate

on:
  pull_request:

jobs:
  validate:
    name: Validate
    uses: eviive/actions/.github/workflows/validate-java.yaml@chore/reusable-workflows
    with:
      java-version: 21
      checkstyle-version: 10.26.1
